<% include ../partials/top.ejs %>

<h1>PhylOTtastic</h1>

<form method="POST">

<p><a href="http://www.phylotastic.org/">Phylotastic</a> on the <a href="http://opentreeoflife.org/">Open Tree of Life</a>.</p>

<p><strong>Step 1.</strong> Enter a list of taxa below.</p>

<div style="display: inline-block"><center>
    <textarea style="margin: auto" name="taxon_names" rows="20" cols="80"><%= taxon_names %></textarea><br>
    <button class="btn btn-large <% if(taxon_names == '') { %>btn-primary<% } %>" style="margin: auto; width:100%;" type="submit">Retrieve OTT identifiers for taxon names</button>
</center></div>

<hr>

<div style="display: inline-block">
    <p><strong>Step 2.</strong> Confirm the Open Tree Taxonomy mapping of
    the taxa you entered above.
    </p>

    <table class="table table-striped table-condensed">
        <thead>
            <tr>
                <th></th>
                <th><input type="checkbox" onclick="$('.selected_names').prop('checked', $(this).prop('checked'));"></th>
                <th>Entered name</th>
                <th>Corrected name</th>
                <th>Mapped to</th>
                <th>Authority</th>
                <th>Regnum</th>
                <th>Links</th>
            </tr>
        </thead>
        <tbody>
            <% index = 0 %>
            <% names.forEach(function(name) { index++; %>
                <tr>
                    <td><%= index %></td>
                    <td>
                        <input type="checkbox" class="selected_names" name="selected_<%= index %>" <% if(name.checked) { %>checked="1"<% } %> />
                    </td>
                    <td>
                        <input type="hidden" name="names" value="<%= name['verbatim'] %>" />
                        <%= name['verbatim'] %>
                    </td>
                    <td>
                        <input type="text" name="corrected_<%= index %>" value="<%= name['corrected'] %>" />
                    </td>
                    <% if(name.mapped_to_ott_id) { %>
                        <td>

                        </td>
                    <% } %>
                </tr>
            <% }); %>
        </tbody>
    </table>

    <input style="margin: auto; width:100%;" type="submit" value="Retrieve tree">
</div>

<hr>

<p><strong>Step 3.</strong> Here's your tree!</p>

<p>In Newick format:</p>

<textarea></textarea>

</form>

<% include ../partials/bottom.ejs %>
